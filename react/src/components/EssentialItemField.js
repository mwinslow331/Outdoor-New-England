import React from 'react';

const EssentialItemField = (props) => {
  return(
    <TextField
      content={this.state.essentialItems}
      label="Essential Items"
      name="essential_items"
      onChange={this.eventEssentialItemsHandle}
    />
  );
}
import
